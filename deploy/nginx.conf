user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log warn;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    #gzip  on;

    upstream gunicorn {
        server terriyaki-go:8080;  # using the docker network
    }

    server {
        # the port your site will be served on
        # listen 9090;

        # port for ssl
        listen [::]:9090 ssl;
        listen 9090 ssl; 

        # ssl certificate
        ssl_certificate /etc/nginx/ssl/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/privkey.pem;

        server_name terriyaki.space;
        charset utf-8;

        client_max_body_size 75M;   # adjust to taste

        # deny unrecognized require
        location ~ /\.ht {
            deny all;
        }

        location /api {
            proxy_pass http://gunicorn;
            
            # Forward real client IP address
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Standard proxy headers
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Port $server_port;
        }

        # # ================================
        # # NOTE: THIS IS FOR WEBSOCKET CONNECTIONS
        # # ================================

        # location /ws {
        #     proxy_pass http://gunicorn;
        #     # this magic is needed for WebSocket
        #     proxy_http_version  1.1;
        #     proxy_set_header    Upgrade $http_upgrade;
        #     proxy_set_header    Connection "upgrade";
        #     proxy_set_header    Host $http_host;
        #     proxy_set_header    X-Real-IP $remote_addr;
        # }

        # ================================
        # NOTE: THIS IS FOR THE FRONTEND
        # ================================
        
        # location / {
        #     root   /usr/share/nginx/html;
        #     index  index.html index.htm;
        #     try_files $uri /index.html;   
        # }

        # ================================
        # NOTE: THIS IS FOR THE STATIC FILES
        # ================================
        
        # location /media/ {
        #     alias /usr/share/nginx/static/;
        # }

    }

}